Use case: precise-object-edit.
Asset type: looping animation sprite sheet for a Pebble Time 2 pixel-art watchface.
Input image 1 is the exact edit target, 200x228 background. Create ONE sprite sheet, exactly 4 columns by 4 rows, 16 equal rectangular tiles, no margins, no gutters, no labels. Overall aspect ratio 200:228 (e.g. 1600x1824). Tiles are frames 0..15 in row-major order of ONE continuous seamless cyclic animation.
Preserve the reference composition extremely faithfully: pure black empty upper-left and middle-left for clock digits, stationary yellow moon at x=166 y=33, fixed black village silhouettes along bottom, blue/cyan/white painted air ribbons hugging right side and curving across lower half. No added stars, objects, text or clock.
Animate ONLY the blue cyan air brushstroke ribbons flowing along their curved paths, with small evolving curls and eddies. Smooth subtle phase increments, not 16 different paintings. Moon disk and village exactly same size and position every tile. Existing wind stream is gently alive: strokes travel, curl and wind around the moon and down right edge, sweeping left across bottom. Last frame transitions naturally to first with same small motion as all other transitions; do not duplicate first frame at end. Fixed camera. Preserve pixel art coarse brushwork and near-black negative space. Strict Pebble RGB palette: each channel only 0,85,170,255, up to 64 colors. Hard crisp pixels, no gradients or antialiasing. Reference is frame-zero composition; keep silhouettes and major wave paths stable across entire sheet.
