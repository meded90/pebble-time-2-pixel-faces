{
  "name": "wrist-agent-server",
  "version": "1.0.0",
  "description": "Private Pebble bridge for ChatGPT Workspace Agents with an MCP result callback",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.23.2"
  },
  "scripts": {
    "start": "node --env-file=.env src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config.js && node --check src/mcp.js && node --check src/store.js && node --check src/workspace-agent-client.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "express": "5.2.1",
    "zod": "4.4.3"
  }
}
